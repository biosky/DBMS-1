use collegeDB

db.createCollection("student")

db.student.insertOne({ roll_no: 1, name: "Anita", course: "CS", marks: 85 })
db.student.insertMany([
  { roll_no: 2, name: "Rahul", course: "IT", marks: 78 },
  { roll_no: 3, name: "Neha", course: "CS", marks: 90 }
])

db.student.find()
db.student.find({ course: "CS" })
db.student.find().pretty()

db.student.updateOne(
  { roll_no: 2 },
  { $set: { marks: 82 } }
)

db.student.deleteOne({ roll_no: 3 })
